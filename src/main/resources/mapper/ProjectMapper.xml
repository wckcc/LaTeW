<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.mapper.ProjectMapper">

    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="org.example.dto.ProjectDTO">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="name" property="name" jdbcType="VARCHAR"/>
        <result column="content" property="content" jdbcType="LONGVARCHAR"/>
        <result column="user_id" property="userId" jdbcType="BIGINT"/>
        <result column="created_at" property="createdAt" jdbcType="TIMESTAMP"/>
        <result column="updated_at" property="updatedAt" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!-- 插入项目 -->
    <insert id="insert" parameterType="org.example.dto.ProjectDTO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO projects (
            name,
            content,
            user_id,
            created_at,
            updated_at
        ) VALUES (
            #{name, jdbcType=VARCHAR},
            #{content, jdbcType=LONGVARCHAR},
            #{userId, jdbcType=BIGINT},
            #{createdAt, jdbcType=TIMESTAMP},
            #{updatedAt, jdbcType=TIMESTAMP}
        )
    </insert>

    <!-- 根据用户ID查询项目列表 -->
    <select id="selectByUserId" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT 
            id,
            name,
            content,
            user_id,
            created_at,
            updated_at
        FROM projects
        WHERE user_id = #{userId, jdbcType=BIGINT}
        ORDER BY updated_at DESC, created_at DESC
    </select>

</mapper>

